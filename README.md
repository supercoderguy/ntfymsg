# ntfymsg
Online messaging service for ntfy.sh

# How to use
1. Run `chmod +x message.sh` if file is not executable (it should be)
2. Now run `./message.sh`, and enter a username (first run only)
3. Start messaging!

# Requirements
1. Requires ntfy.sh app
2. Latest version of `bash` and `python3`